image: "spritecloud/ruby-selenium-runner"

variables:
  REVISION: $(git rev-list --count HEAD)

services:
  - selenium/standalone-chrome:latest

variables:
  selenium_remote_url: "http://selenium__standalone-chrome:4444/wd/hub/"

before_script:
- bundle install
- echo $REVISION
- REVISION2 = $(git rev-list --count HEAD)
- echo $REVISION2

cucumber:
  script:
    - cucumber